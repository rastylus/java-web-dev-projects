package org.example;

public class WordLength {
    static String[] word;
    public static length(String[] word) {
        if(word.length() == 5) {
            return word.length();

        }
    }
}